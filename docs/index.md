# Documents for rest-service

This RESTful API service has two endpoints:

- `/health/`
- `/api/v1/info/`

TODO: explain what each endpoint does

# How to access the service?

You can access the service by visiting the URL https://rest-service.net/api/v1/info/

